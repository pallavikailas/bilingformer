model_path = "/path/to/mt5/model"  # Path to the pre-trained model or model checkpoint
max_length = 16  # Maximum length of the input sequences
batch_size = 32  # Batch size for training and evaluation
num_epochs = 30  # Number of epochs to train the model
learning_rate = 2e-5  # Learning rate for the optimizer
step_size = 10  # Step size for the learning rate scheduler
gamma = 0.1  # Gamma value for learning rate decay
